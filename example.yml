version: 1
proxies:
  unsplash:
    host: "https://api.unsplash.com"
    cache:
      ttl: 5m

    request_headers:
      add:
        - key: Authorization
          value: "Client-ID %UNSPLASH_API_KEY%"

        - key: "Accept-Version"
          value: "v1"
