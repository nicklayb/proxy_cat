version: 1
proxies:
  unsplash:
    host: "https://api.unsplash.com"
    request_headers:
      add:
        - key: Authorization
          value: "Client-ID %UNSPLASH_API_KEY%"

        - key: "Accept-Version"
          value: "v1"

